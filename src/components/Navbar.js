import React, { Component } from 'react';
import SearchBar from "./SearchBar"

class Navbar extends Component {
    render() {
        return (
            <div>
                SUP BITCHES IM THE NAVBAR!
                <SearchBar />
            </div>
        );
    }
}

export default Navbar;